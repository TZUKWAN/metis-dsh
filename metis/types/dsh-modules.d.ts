/**
 * Deliberately empty migration marker.
 *
 * METIS now typechecks directly against the DeepSeek Harness declaration files
 * configured in ../tsconfig.json. Do not reintroduce ambient declarations here:
 * they would mask the real Cordis/DSH contract with structural `any` types.
 */
