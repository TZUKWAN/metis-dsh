# Local runtime/test residue — never commit.
.runtime-verify-state.json
.test-*
metis-data/
node_modules/

# Build outputs (reproducible via pnpm run build:plugins / pack:plugins)
dist/
dist-tarballs/
